(jbuild_version 1)

(menhir
    ((flags ())
     (modules (parser ast.ml))))

(ocamllex (lexer))

(library
 (
  (name diffgit)
  (library_flags (-linkall))
  (libraries (core util base str))
  (preprocess (pps (ppx_jane)))
 )
)
